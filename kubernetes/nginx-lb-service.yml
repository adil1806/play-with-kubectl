apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-app
spec:
  selector:
   app: nginx-app
  type: LoadBalancer
  externalIPs:
  - 3.20.66.155
  ports:
   - nodePort: 31000
     port: 80
     targetPort: 80

