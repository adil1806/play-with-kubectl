kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
     name: allow-from-prod
spec:
 podSelector:
    matchLabels:
          app: mysql
 ingress:
  - from:
    - namespaceSelector:
           matchLabels:
              purpose: production

