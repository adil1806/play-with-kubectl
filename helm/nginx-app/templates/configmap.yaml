apiVersion: v1
kind: ConfigMap
metadata:
    name: {{ include "my.name" . }}-config
    labels:
       tag: {{ .Values.image.tag }}
       env: {{ .Values.env }}
data:
   index.html: |
{{ .Files.Get (printf "env/application-%s.yml" .Values.env)  | indent 6 }}
